apiVersion: v1
kind: Pod
metadata:
  name: cpu-stress
  labels:
    test: stress
spec:
  containers:
  - name: cpu-stress
    image: polinux/stress-ng
    command: ["stress-ng"]
    args:
      - "--cpu"
      - "8"
      - "--cpu-load"
      - "100"
      - "--timeout"
      - "0"
